📌 ЧАСТЬ 1: ОБЩАЯ АРХИТЕКТУРА СИСТЕМЫ
Что делает этот код?
Это интеллектуальная система оценки инвестиционных рисков, которая:

Анализирует фундаментальные показатели всех акций в портфеле

Автоматически определяет "нормальные" значения для каждого показателя

Классифицирует акции по 4 уровням риска на основе статистических отклонений

Обучает нейросеть предсказывать эти категории риска

Дает рекомендации по управлению портфелем

Почему это важно?
Традиционные методы используют жесткие пороги:

✅ P/E < 10 = хорошо

❌ P/E > 20 = плохо

Но рынок постоянно меняется! Сегодня P/E=15 может быть нормой, а завтра - переоценкой.

Наш подход:

📊 Смотрим на реальное распределение всех компаний

📈 Определяем медиану и процентили

🎯 Ранжируем компании относительно друг друга

📌 ЧАСТЬ 2: ПОЭТАПНЫЙ РАЗБОР КОДА
ЭТАП 1: ПОДГОТОВКА ПРИЗНАКОВ (функция prepare_features_for_nn)
python
def prepare_features_for_nn(df):
    """Подготовка признаков для нейросети с использованием статистических методов"""
🔍 Что происходит на этом этапе?
1.1 Определение всех возможных признаков

python
all_potential_features = [
    "P/E", "P/B", "P/S", ...  # Валютные мультипликаторы
    "ROE", "ROA", "NPM", ...   # Показатели рентабельности
    "Debt/Capital", ...        # Показатели долга
    "dividend_yield", "EPS", "Beta"  # Дивиденды и риск
]
Логика: Мы заранее знаем, какие показатели важны для анализа, но не знаем, какие из них есть в конкретном файле Excel.

1.2 Умный поиск признаков по алиасам

python
feature_aliases = {
    "P/E": ["P/E", "P_E", "PE", "Price/Earnings"],
    "ROE": ["ROE", "Рентабельность капитала"],
    ...
}
Логика: В разных источниках данные называются по-разному. Мы создаем словарь синонимов и ищем все возможные варианты.

1.3 Расчет статистики ДО нормализации

python
for feature in available_features:
    # Собираем ВСЕ значения этого признака
    values = []
    for alias in feature_aliases.get(feature, [feature]):
        if alias in df.columns:
            vals = df[alias].dropna()
            for v in vals:
                try:
                    values.append(float(v))
                except:
                    pass
    
    # ОЧИЩАЕМ от экстремальных выбросов для расчета статистики
    q1 = np.percentile(values, 25)
    q3 = np.percentile(values, 75)
    iqr = q3 - q1
    lower_bound = q1 - 3 * iqr
    upper_bound = q3 + 3 * iqr
    
    # Берем только "нормальные" значения для расчета медианы
    clean_values = values[(values >= lower_bound) & (values <= upper_bound)]
Критически важная логика:

Мы сначала собираем все значения признака по всем компаниям

Отсекаем явные выбросы (значения, выходящие за 3 IQR)

По очищенным данным считаем медиану, среднее, квартили

Это дает нам объективную рыночную норму

Пример:

text
P/E всех компаний: [4, 6, 8, 10, 12, 15, 20, 500]
После очистки: [4, 6, 8, 10, 12, 15, 20] (удалили 500)
Медиана = 10
Мы не позволяем одному выбросу исказить всю статистику!

1.4 Формирование векторов признаков

python
for idx, row in df.iterrows():
    feature_vector = []
    
    for feature in available_features:
        # Ищем значение по всем алиасам
        for alias in feature_aliases.get(feature, [feature]):
            if alias in row and pd.notna(row[alias]):
                value = float(row[alias])
                break
        
        if value is None:
            # Если нет значения - используем МЕДИАНУ по рынку
            value = feature_stats[feature]['median']
        
        # Нормализуем экстремальные значения
        if feature in ['P/E', 'P/B', ...] and value > 100:
            value = 100  # Обрезаем сверху
Логика:

Если у компании нет какого-то показателя - мы не отбрасываем её, а подставляем медиану по рынку

Это позволяет сохранить выборку и не терять данные

Экстремальные значения обрезаются до разумных пределов

ЭТАП 2: СТАТИСТИЧЕСКАЯ ОЦЕНКА РИСКА (функция calculate_risk_categories_statistical)
Это САМАЯ ВАЖНАЯ часть кода!

python
def calculate_risk_categories_statistical(df, X, feature_names, feature_stats, use_ae_scores=True):
    """Рассчет категорий риска на основе статистических методов"""
2.1 Логика оценки каждого показателя
Для "чем меньше, тем лучше" (P/E, P/B, Debt/EBITDA):

python
if value <= 0:
    risk = 3  # Отрицательное значение = катастрофа
elif value <= median * 0.7:
    risk = 0  # На 30% лучше медианы = отлично
elif value <= median:
    risk = 1  # Лучше медианы = хорошо
elif value <= median * 1.5:
    risk = 2  # Хуже медианы до 50% = плохо
else:
    risk = 3  # На 50%+ хуже медианы = очень плохо
Почему именно такие коэффициенты?

0.7 (30% лучше медианы) - статистически значимое преимущество

1.5 (50% хуже медианы) - точка, после которой начинаются проблемы

Эти коэффициенты не взяты с потолка - это стандартные статистические пороги

Для "чем больше, тем лучше" (ROE, ROA, dividend_yield):

python
if value <= 0:
    risk = 3
elif value >= median * 1.3:
    risk = 0  # На 30% лучше медианы
elif value >= median:
    risk = 1  # Лучше медианы
elif value >= median * 0.7:
    risk = 2  # Хуже медианы до 30%
else:
    risk = 3  # Значительно хуже медианы
Для Beta коэффициента (специальная логика):

python
if value < 0.5:
    risk = 1  # Низкая волатильность = хорошо
elif value < 1.0:
    risk = 0  # Идеально - следует за рынком
elif value < 1.5:
    risk = 1  # Умеренная волатильность
elif value < 2.0:
    risk = 2  # Высокая волатильность
else:
    risk = 3  # Экстремальная волатильность
Почему Beta имеет другую логику?
Beta = 1.0 - это идеал (акция движется синхронно с рынком)

Меньше 1 - защитная акция (меньше риска)

Больше 1 - агрессивная акция (больше риска)

2.2 Весовая система
python
weights = []
for j, feature in enumerate(feature_names):
    if feature in ['P/E', 'ROE', 'dividend_yield', 'Debt/EBITDA']:
        weight = 2.0  # КРИТИЧЕСКИ важные показатели
    elif feature in ['P/B', 'P/S', 'ROA', 'NPM', 'EV/EBITDA']:
        weight = 1.5  # ОЧЕНЬ важные показатели
    elif feature in ['Beta', 'Бета', 'EPS']:
        weight = 1.2  # ВАЖНЫЕ показатели
    else:
        weight = 1.0  # БАЗОВЫЕ показатели
Обоснование весов:

P/E - главный показатель доходности

ROE - главный показатель эффективности

dividend_yield - реальная доходность для инвестора

Debt/EBITDA - главный показатель долговой нагрузки

Эти 4 показателя получают двойной вес, потому что они наиболее сильно коррелируют с реальными рисками.

2.3 Определение категорий через процентили
python
# После расчета рисков для всех компаний
p25 = np.percentile(y, 25)  # 25% компаний имеют риск НИЖЕ этого значения
p50 = np.percentile(y, 50)  # Медиана - "типичный" риск
p75 = np.percentile(y, 75)  # 25% компаний имеют риск ВЫШЕ этого значения

# Категоризация:
if risk_score <= p25:    category = 0  # Топ-25% по надежности
elif risk_score <= p50:  category = 1  # 25-50% - выше среднего
elif risk_score <= p75:  category = 2  # 50-75% - ниже среднего
else:                    category = 3  # 75-100% - самые рискованные
Гениальность подхода:

Мы не говорим: "P/E=10 - это хорошо"

Мы говорим: "Эта компания в топ-25% по надежности"

Это абсолютно объективно и адаптируется под текущий рынок

ЭТАП 3: НОРМАЛИЗАЦИЯ ДАННЫХ
python
# Вместо StandardScaler используем RobustScaler
scaler = RobustScaler()
X_scaled = scaler.fit_transform(X)
Почему RobustScaler?

StandardScaler	RobustScaler
(x - mean) / std	(x - median) / IQR
Чувствителен к выбросам	Устойчив к выбросам
Среднее = 0, Std = 1	Медиана = 0, IQR = 1
Пример:

text
Данные: [1, 2, 3, 4, 100]
StandardScaler: экстремальное значение 100 исказит всё
RobustScaler: медиана=3, IQR=2, значение 100 станет ~48.5
Для финансовых данных с их выбросами RobustScaler - единственный правильный выбор!

ЭТАП 4: АНСАМБЛЬ НЕЙРОСЕТЕЙ
python
def create_advanced_risk_assessment_nn(input_shape, num_classes=4):
    # Модель 1: Классическая с регуляризацией
    # Модель 2: С residual connections
    # Модель 3: Широкая сеть
Зачем 3 разные модели?

Закон мудрости толпы (Wisdom of the Crowd):

Одна модель может ошибаться

Три разные модели ошибаются по-разному

Усреднение их предсказаний гасит ошибки

Модель 1 - Классическая:

Простая, быстрая, надежная

L2-регуляризация против переобучения

BatchNormalization для стабильности

Модель 2 - Residual:

Может обучаться глубже

Residual connections спасают от затухания градиента

Лучше находит сложные зависимости

Модель 3 - Широкая:

Много нейронов в первом слое

Хорошо "впитывает" информацию

Компенсирует недостатки первых двух

ЭТАП 5: КРОСС-ВАЛИДАЦИЯ
python
skf = StratifiedKFold(n_splits=n_folds, shuffle=True, random_state=42)
Зачем это нужно?

Мы не можем проверить нейросеть на тех же данных, на которых учили - она их запомнит, а не обучится.

Стратифицированная K-Fold кросс-валидация:

Делим данные на K частей

K-1 частей используем для обучения

1 часть используем для проверки

Повторяем K раз, каждый раз с разной проверочной частью

Усредняем результаты

Stratified = сохраняем пропорции классов в каждой части

ЭТАП 6: ПРЕДСКАЗАНИЕ И АГРЕГАЦИЯ
python
# Каждая модель делает предсказание
val_pred = model.predict(X_val)

# Усредняем предсказания всех моделей
avg_pred = np.mean(fold_model_predictions, axis=0)

# Берем класс с максимальной вероятностью
y_pred = np.argmax(ensemble_predictions, axis=1)

# Уверенность = максимальная вероятность
confidence = np.max(ensemble_predictions[i])
Как это работает:

Модель выдает 4 вероятности (сумма = 1.0)

Пример: [0.7, 0.2, 0.1, 0.0]

Предсказание: класс 0 (низкий риск)

Уверенность: 70%

ЭТАП 7: ИНТЕРПРЕТАЦИЯ РЕЗУЛЬТАТОВ
python
# Категории риска:
0 - A: Низкий риск     (топ-25%)
1 - B: Средний риск    (25-50%)
2 - C: Высокий риск    (50-75%)
3 - D: Очень высокий риск (75-100%)
Важно понимать:

Категория не абсолютна, а относительна

"Низкий риск" = "лучше, чем 75% других компаний"

"Высокий риск" = "хуже, чем 75% других компаний"

📌 ЧАСТЬ 3: ПРИМЕР РАБОТЫ СИСТЕМЫ
Дано:
Рынок из 100 компаний с P/E от 5 до 30

Шаг 1: Расчет статистики

text
Медиана P/E = 15
25-й процентиль = 10
75-й процентиль = 20
Шаг 2: Оценка компании А (P/E = 8)

text
Сравнение с медианой: 8 < 15 → лучше рынка
Сравнение с 25-м процентилем: 8 < 10 → в топ-25%
Риск по P/E = 0 (низкий)
Шаг 3: Оценка компании Б (P/E = 25)

text
Сравнение с медианой: 25 > 15 → хуже рынка
Сравнение с 75-м процентилем: 25 > 20 → в bottom-25%
Риск по P/E = 3 (очень высокий)
Шаг 4: Агрегация всех показателей

text
Компания А: 
- P/E риск = 0 (вес 2.0)
- ROE риск = 0 (вес 2.0)  
- Долг риск = 1 (вес 1.5)
Итого: (0*2 + 0*2 + 1*1.5) / (2+2+1.5) = 1.5/5.5 = 0.27

Компания Б:
- P/E риск = 3 (вес 2.0)
- ROE риск = 2 (вес 2.0)
- Долг риск = 2 (вес 1.5)
Итого: (3*2 + 2*2 + 2*1.5) / 5.5 = 13/5.5 = 2.36
Шаг 5: Определение категорий

text
Распределение рисков по рынку:
p25 = 0.5
p50 = 1.2  
p75 = 2.0

Компания А: 0.27 ≤ p25 → Категория A (Низкий риск)
Компания Б: 2.36 > p75 → Категория D (Очень высокий риск)
📌 ЧАСТЬ 4: ПОЧЕМУ ЭТО ЛУЧШЕ ТРАДИЦИОННЫХ МЕТОДОВ?
Традиционный подход:
python
if p_e < 10: risk = "Low"
elif p_e < 20: risk = "Medium"  
else: risk = "High"
Проблемы:

❌ Не учитывает секторальные особенности

❌ Не адаптируется к рыночным циклам

❌ Не видит общей картины рынка

❌ Субъективные пороги

Наш подход:
python
# Статистический анализ ВСЕХ компаний
p25, p50, p75 = np.percentile(all_p_e, [25, 50, 75])

if p_e <= p25: risk = "Low"      # Топ-25%
elif p_e <= p50: risk = "Medium" # 25-50%
elif p_e <= p75: risk = "High"   # 50-75%
else: risk = "Very High"         # 75-100%
Преимущества:

✅ Объективность - пороги диктует сам рынок

✅ Адаптивность - работает в любом рынке

✅ Контекст - учитывает распределение всех компаний

✅ Интерпретируемость - "лучше чем X% компаний"

📌 ЧАСТЬ 5: ПРАКТИЧЕСКОЕ ПРИМЕНЕНИЕ
Для инвестора:
Низкий риск (A):

"Эта компания входит в топ-25% по надежности"

Можно рассматривать для ядра портфеля

Рекомендуемая доля: 5-15%

Средний риск (B):

"Типичный представитель рынка"

Подходит для диверсификации

Рекомендуемая доля: 3-8%

Высокий риск (C):

"Компания в зоне неопределенности"

Только для агрессивных стратегий

Рекомендуемая доля: 1-3%

Очень высокий риск (D):

"Экстремальные отклонения от нормы"

Спекулятивный инструмент

Рекомендуемая доля: 0-1%

Для трейдера:
A, B - можно держать долгосрочно

C - искать точку входа, стоп-лосс ближе

D - только краткосрочные идеи, жесткие стопы

Для риск-менеджера:
Мониторинг миграции компаний между категориями

Если компания из A перешла в D → красный флаг

Автоматическое ребалансирование портфеля

🎯 ИТОГ: ФИЛОСОФИЯ ПОДХОДА
Этот код реализует принцип "Статистического превосходства":

Мы не знаем, какое P/E "хорошее"

Но мы знаем, что компания с P/E=8 лучше 75% рынка

И мы знаем, что компания с P/E=30 хуже 75% рынка

Этого достаточно для принятия решений

Ключевая мысль:

В мире инвестиций нет абсолютных истин, есть только относительные преимущества.
Наша задача - не найти "идеальную" компанию, а найти компании, которые значительно лучше среднего.

Система автоматически:

Определяет, что такое "среднее" на текущем рынке

Находит компании, которые лучше этого среднего

Ранжирует их по степени превосходства

Дает практические рекомендации